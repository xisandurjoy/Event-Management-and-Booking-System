# ============================================================
#  MySQL Profile (production / default)
# ============================================================
spring.datasource.url=jdbc:mysql://localhost:3306/shrabon_events?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Dhaka&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=248888
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Initialize seed data via Java DataInitializer (schema handled by Hibernate)
spring.sql.init.mode=never
